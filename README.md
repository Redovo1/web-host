# WebHostMost 自动保号脚本

> 一个每月自动帮你登录 [WebHostMost](https://client.webhostmost.com) 免费虚拟主机的脚本，防止账号因长期不登录被删除。

## 这是什么？

WebHostMost 提供免费的虚拟主机，但要求用户**定期登录**，否则账号会被删除（俗称"保号"）。

这个项目的作用是：**每月 11 号自动帮你登录一次**，你什么都不用管，也不用记着去登录。

- ✅ 支持多个账号一起保号
- ✅ 完全免费（用 GitHub Actions 跑，不用自己的电脑）
- ✅ 登录成功或失败都会记录在日志里，失败时 GitHub 会发邮件通知你

---

## 保姆级配置教程

跟着做就行，每一步都有截图指引（用文字描述）。全程大概 **5 分钟**。

### 第 0 步：准备一个 GitHub 账号

如果你还没有 GitHub 账号：

1. 打开 [https://github.com/signup](https://github.com/signup)
2. 填邮箱、设置密码、起个用户名
3. 去邮箱收验证码，完成注册

已经有账号就跳过这步。

---

### 第 1 步：把这个项目复制到你自己的账号下（Fork）

"Fork" 的意思就是**把别人的项目复制一份到你自己的 GitHub 账号**，这样你就能自己改、自己用。

1. 打开项目地址：[https://github.com/Redovo1/web-host](https://github.com/Redovo1/web-host)
2. 看页面**右上角**，找到一个写着 **"Fork"** 的按钮，点它
3. 在新打开的页面上，直接点绿色的 **"Create fork"** 按钮（什么都不用改）
4. 等几秒钟，页面会自动跳转到**你自己账号下的** `web-host` 项目

> 怎么确认成功了？看页面左上角，如果显示的是 `你的用户名/web-host`，就说明 fork 成功了。

---

### 第 2 步：配置你的 WebHostMost 账号密码（Secret）

这一步是告诉脚本：**你要帮我保号哪些账号**。

1. 在你刚 fork 的仓库页面，点**最上面一排标签**里的 **"Settings"**（设置，在最右边）
2. 看**左侧菜单**，找到 **"Secrets and variables"** 点开 → 再点下面的 **"Actions"**
3. 点页面中间绿色的 **"New repository secret"** 按钮
4. 填两个输入框：
   - **Name**（名字）：填 `WEBHOST`（注意全大写，一个字母都别错）
   - **Secret**（值）：填你的账号密码，格式是 `邮箱:密码`

   **举例**：假设你有两个 WebHostMost 账号：
   - 账号1：邮箱 `zhangsan@gmail.com`，密码 `abc123`
   - 账号2：邮箱 `lisi@qq.com`，密码 `xyz789`

   那你就在 **Secret** 框里填：
   ```
   zhangsan@gmail.com:abc123
   lisi@qq.com:xyz789
   ```
   （一行一个账号，中间用英文冒号 `:` 隔开邮箱和密码）

   只有一个账号就只填一行。

5. 点绿色的 **"Add secret"** 按钮保存

> ⚠️ 密码里**不要有空格**，如果你的密码里有 `:` 冒号，也可能出问题，最好去 WebHostMost 把密码改成只有字母和数字的。

---

### 第 3 步：给脚本"写入"权限

脚本每次跑完需要在仓库里留个时间戳记录（防止 GitHub 60 天不动就禁用），所以要给它写入权限。

1. 还是在 **Settings** 页面
2. 左侧菜单点 **"Actions"** → 下面的 **"General"**
3. 滚动到页面**最下面**，找到 **"Workflow permissions"** 这一栏
4. 选择 **"Read and write permissions"**（读和写权限）
5. 点下方的 **"Save"** 按钮保存

---

### 第 4 步：开启 Actions（定时任务）

1. 点最上面一排标签里的 **"Actions"**
2. 如果看到一个黄色的提示说 workflow 被禁用了，点绿色的 **"I understand my workflows, go ahead and enable them"** 按钮启用
3. 如果没看到提示，就说明已经启用了，直接下一步

---

### 第 5 步：手动跑一次，测试能不能用

不用等到 11 号，先手动跑一次看看：

1. 还在 **Actions** 页面，**左侧**会看到一个叫 **"Run Login Script"** 的任务，点它
2. 页面右边会出现一个 **"Run workflow"** 的灰色按钮，点一下
3. 弹出小框，直接点里面的绿色 **"Run workflow"** 按钮
4. 等大概 **1-2 分钟**，刷新一下页面
5. 看任务状态：
   - ✅ **绿色对勾**：登录成功，配置完成！
   - ❌ **红色叉**：有账号登录失败，点进去看看是哪个账号的问题（一般是密码填错了）

**怎么看详细日志？**

1. 点那次任务的标题进去
2. 点左边的 **"login"**
3. 展开 **"运行登录脚本"** 这一步
4. 能看到每个账号的登录结果，比如：
   ```
   账号 `zhangsan@gmail.com` 登录成功 ✅
   账号 `lisi@qq.com` 登录失败: Invalid email or password
   ```

---

### 第 6 步：就这样了，以后不用管

配置完就结束了。之后每月 **11 号**会自动登录一次（北京时间 11:22 左右），你什么都不用做。

**想收到失败通知？** GitHub 默认就会发邮件，如果没收到可以检查一下：

1. 点**右上角自己头像** → **Settings**
2. 左侧 **"Notifications"**
3. 找到 **"Actions"**，确保**邮件通知**是打开的

这样一旦有账号登录失败，GitHub 会发邮件到你注册时用的邮箱。

---

## 常见问题

### Q: 我想换个日期跑，不想 11 号跑怎么办？

编辑文件 `.github/workflows/login.yaml`，找到这一行：

```yaml
- cron: "22 3 11 * *"
```

五个数字的含义分别是：**分 时 日 月 星期**（用的是 UTC 时间，北京时间 = UTC + 8 小时）

比如：
- 每月 1 号跑：`"22 3 1 * *"`
- 每月 15 号跑：`"22 3 15 * *"`
- 每周一跑：`"22 3 * * 1"`

### Q: 我要再加一个账号怎么办？

去 **Settings → Secrets and variables → Actions**，找到 `WEBHOST` 那一行，点右边的铅笔图标编辑，在下面加一行新账号就行。

### Q: 我想删掉一个账号怎么办？

同上，编辑 `WEBHOST`，删掉对应那一行。

### Q: 脚本跑失败了，红叉，怎么办？

最常见的原因：
1. **密码填错了** → 重新编辑 `WEBHOST`，改对密码
2. **账号已经被删了** → 去 WebHostMost 重新注册
3. **WebHostMost 网站暂时挂了** → 过几小时再手动跑一次试试（第 5 步）

### Q: 我的隐私安全吗？密码放在 GitHub 会不会被偷？

- Secrets 是**加密保存**的，日志里也不会显示
- 只有你自己能看到和修改
- 就算被偷，对方也只能拿到 WebHostMost 免费主机的登录权限（没啥值钱的），改个密码就行

---

## 出问题了怎么办？

- 看 [Actions 页面](../../actions) 的日志找原因
- 实在搞不定，可以去项目 [Issues](../../issues) 提问

祝保号顺利 🎉
